server:
  port: 80
cors:
  originPatterns: http://localhost:3000,http://localhost:8080,https://example.com.br
file:
  upload-dir: C:/Users/Bruno/Downloads/UploadDir
security:
  jwt:
    token:
      secret-key: 53cr37
      expire-length: 3600000
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://rest-with-spring-boot.cl660weswdnt.us-east-1.rds.amazonaws.com:5432/rest_with_spring_boot
    username: postgres
    password: admin123
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    show-sql: false
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2KB
      max-file-size: 200MB
      max-request-size: 215MB
springdoc:
  pathsToMatch:
    - /auth/**
    - /api/**/v1/**
  swagger-ui:
    use-root-path: true
